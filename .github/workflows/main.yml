name: ArchiveRepository
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Zip Release
      uses: TheDoctor0/zip-release@v0.2.1
      with:
        filename: 'LaravelPackage.zip'
        exclusions: '*.git* /*node_modules/* .editorconfig'
